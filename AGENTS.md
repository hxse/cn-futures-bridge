# AGENTS.md

先明确意图与边界，再实现和验证。文档服务于准确协作，不维护重复的流程记录。本文件分为跨语言通用规范和本项目规范；用户本轮明确的范围、操作与交付要求优先。

## 一、通用规范

### 1. 沟通与一次性授权

* 全程中文沟通。文档文件名使用英文；正文和章节标题以中文为主，关键术语、类型名和概念可保留英文。代码标识使用英文，代码注释使用中文。
* 先讲问题、原因和处理办法，再给术语与证据；短例子服务理解，不用路径、缩写或零碎清单代替解释。
* 默认只给方案，可以做必要的只读检查。“赞同”“可以”“这个设计合理”等只确认观点，不是执行授权；讨论、评估、审查和任何不清晰的指令一律按只读处理，不主动编辑受控内容或改写任务历史。
* 只读审阅允许必要的工作区定位；代码审阅可以进行不改变受控内容的必要验证，生成测试缓存或构建产物不等于修改源码。先核对检查入口是否会自动格式化或改写受控生成文件；会写入时，使用现有不改写内容的正式入口，或说明所需写入范围并取得授权，不能借“运行检查”自动修改内容。
* 纯文档任务默认只核对内容、边界、示例逻辑、引用和一致性，不运行构建、测试、lint、类型检查等验证命令。即使文档描述了算法、测试契约或可执行示例，也不因此自动执行；只有用户明确要求时才运行。spec 写将来怎样验证，不代表编写 spec 时就执行验证。

| 用户明确要求 | 本次执行范围 |
| --- | --- |
| 讨论、方案，或未要求落盘 | 聊天中说明方案，不落盘。 |
| 保存草稿 | 只在草稿目录保存文档，不改代码、不创建正式任务或 change/commit。 |
| 修改文档、更新指定文件 | 只修改明确指定的文档范围，不顺手实现代码或迁移其他文件。 |
| 生成正式任务 | 建立 task spec 与必要的本地 change/commit；未要求代码时只落地文档。 |
| 实现、修复、落地代码 | 按明确范围实现、补测和验证；不自动创建无关任务。 |

* 授权只覆盖当前请求及其必要步骤，不是只允许一条命令，也不延续为后续默认模式。“继续”或 `Continue` 只完成之前尚未完成的明确命令，不开新任务、不扩大范围；之前只是讨论，继续仍是讨论。
* 用户明确同时要求文档和代码时可连续完成。实现授权默认包含范围内的测试、生成声明及受影响的 current spec/guide 同步，均限于已批准设计，不包括文档体系迁移、无关任务重写或新的设计决定。
* 用户明确限定“只改代码”时不修改文档，并在回复中说明必要同步尚未进行；不能把未提及文档误解成禁止正常配套同步，也不能用配套同步扩大范围。
* 一般流程：方案 → 正式任务文档 → 授权后实现。复杂任务可先经过草稿。已有合适 task spec 的代码修复直接在授权范围内处理，不机械重走流程。
* 观点确认与执行授权分开；普通实现细节自主处理，低风险假设须说明。涉及设计分叉、任务归属或实施冲突时，按通用规范第 6 章判断，不用假设代替关键决策。
* `AGENTS.md` 维护使用专用或既有 chore，不创建 task spec；修改后交用户完整审阅，不擅自发布。

### 2. 文档职责与草稿

| 文档 | 职责与默认位置 |
| --- | --- |
| current spec | 当前工作区有效的接口、行为、约束和重要测试规则；`doc/current_specs/`。 |
| task spec | 某任务的目标、边界、设计与验证要求；`doc/task_specs/`。任务本身可以同时包含文档和代码。 |
| guide | 必要的用户入口和使用说明；`doc/guides/`，不是每个任务都必须新增。 |
| 草稿 | 尚在讨论的方案，只放文档；默认 `doc/drafts/`，不纳入版本控制。 |

* 判断实际行为看当前工作区源码；判断约定行为看 current spec。二者冲突必须指出，不默默把实现缺陷写成规范。task spec 只说明该任务的设计，不覆盖后续已实施的 current spec。
* 只有文档、尚未实施的新设计留在 task spec，不提前写成 current spec 的现行行为。实现变更后只同步受影响的 current spec，用户用法变化时同步 guide。
* 每个正式任务保留 meta、context、spec；不创建独立 execution/review 文档，不维护手工 index 或任务生命周期状态。meta 负责定位与定级，context 负责场景和取舍，spec 负责具体设计；执行安排自行组织，实际结果按回复规范报告。
* 用户要求保存草稿但未指定目录时使用默认目录；项目可明确指定另一默认目录。用户指定项目外路径时仅本次使用，下次未指定仍回默认目录。
* 项目内草稿目录须加入 `.gitignore`。JJ 项目可在足够早的合适 change 中加入规则，再正常重基后代；它只覆盖该 change 及其后代，切到更早 revision 时须确认忽略仍有效，不强制额外维护本地排除规则。
* 忽略规则不会取消已有文件跟踪，须确认草稿路径未被历史版本跟踪；已有跟踪内容的迁移或取消跟踪须单独明确。忽略目录不是备份，不因切换任务而覆盖或纳入版本控制草稿。
* 草稿通常短期留存，但只有用户明确要求才删除，不因任务完成或临时文件清理自动删除。
* 正式文档须独立自述，不引用或依赖草稿和聊天记忆。草稿中已确定且属于任务范围的场景、取舍与 spec 必须完整转写进正式文档，不遗漏设计重点；删除草稿不应影响理解、实施和验证。
* 历史文档不因新格式要求自动回改。目录迁移、旧文档删除另行明确范围；先保留仍有效的独有规范，不为统一外观批量重写全部历史。

### 3. meta、context 与 spec 固定格式

每个正式任务使用以下文件结构，默认只需前三个文件；`spec_*` 扩展仅在需要拆分时创建：

```text
doc/task_specs/
└── 20260911A configuration normalization/
    ├── meta.md                 # 任务定位、星级与关联
    ├── context.md              # 使用场景、方案对比与取舍
    ├── spec.md                 # 主规范，固定四部分
    ├── spec_01_api.md          # 按需扩展：接口细节
    └── spec_02_algorithm.md    # 按需扩展：算法细节
```

主任务和子任务内部均采用此结构，各自任务目录直接平铺在 `doc/task_specs/` 下，不相互嵌套。

#### 3.1 文档格式

`meta.md` 负责定位，不复制设计正文，不记录进度、验证结果或易变化的 commit hash。使用以下格式，子任务填写主任务 ID；确有替代关系时可增加一行并限定替代范围：

```md
# 20260911A 配置解析统一

- 概括：统一配置解析入口，清理重复校验。
- 级别：二星；涉及多个配置调用入口及回归验证。
- 影响范围：配置模块、公开入口、相关测试。
- 主任务：无。
- 前置任务：无。
- 相关 current spec：doc/current_specs/configuration.md
```

`context.md` 只解释为什么，使用以下结构；没有有价值的替代方案时简述选择理由，不为凑对比发明方案：

```md
# 配置解析统一

## 使用场景与设计初衷

说明谁在什么场景遇到什么问题，以及希望达到的效果。

## 方案对比

比较真正可行的方案，说明各自收益、成本和限制。

## 取舍结论

说明为什么选择当前方案、为什么不选其他方案。
```

默认一个任务使用一份 `spec.md`，固定四部分，直接给出完整设计蓝图；精确到足以实现与验证，不重复 context 的论证，不写成代码说明书。编写时就确定范围、停止线、验证与阶段过渡，不留到实施时临时决定。需要扩展文件时，`spec.md` 仍是唯一阅读入口：

```md
# 配置解析统一

## 任务边界

明确交付物、允许改变与必须保留的行为，以及范围外事项。
在已批准范围内默认直接替换被本任务取代的旧设计，不自动保留兼容包装、
双签名或承担同一职责的平行链路；例外须在阶段过渡中明确批准。
列清应迁移或清理的旧入口、实现、类型、字段、调用方、测试、生成声明与文档。
破坏性更新必须精准，不全盘推翻，不改动无关算法或删除仍有独立职责的实现。
明确停止线：达到哪些具体结果即可交付，哪些问题阻断交付，
哪些限制已明确接受或由后续阶段承接，不将“全面清理”等目标无限延伸。

## 任务规范

定义最终唯一的正式链路、所有权、内部契约、核心流程、必要算法、
不变量、生产校验的时机与规则、边界条件和失败语义；不展开普通实现细节。
用户可见的接口契约放入下一部分，同一规则只完整定义一次。

## 公开接口与用户写法

冻结最终用户或外部调用方可见的 API、DSL、CLI、配置、参数、输入输出及结果访问方式。
提供真实主链正例、关键反例及预期失败方式，不能只给字段清单或用伪代码冒充最终写法。
无公开接口变化时明确说明；纯内部任务的必要调用或行为示例放入“任务规范”，
不为凑格式发明公开接口。

## 测试、验证与阶段过渡

提前确定本任务必须验证的功能、调用方与交互范围、验证命令和通过要求，
覆盖正向成功与反向报错，证明新链路正确、旧写法按约定退出。
范围可按功能、模块和交互表达，不必穷举尚未实现的用例名。
阶段过渡须明确批准：需要维持使用能力或支撑验证时安排最小桥接，
允许暂不可用时说明受影响入口；桥接保持唯一真值，不形成独立维护的新旧双轨。
列明桥接、入口暂不可用、测试阶段性失败或暂不可执行的具体范围、原因、
处理方式、承接阶段，以及桥接退出和能力恢复条件。
基础构建或测试入口受影响时也须提前安排，必要时调整任务拆分或桥接，
不得笼统写“其他测试以后处理”，或在执行失败后再把必须验证的内容推给后续。
默认使用离线自动化测试；用户明确授权新增在线测试时，写清与离线测试分开的入口。
```

* 参数、数据、类型和前置条件的校验属于“任务规范”，须说明校验时机、规则和失败语义；复杂时可设“校验与失败语义”小节，不独立成另一类文档。“测试、验证与阶段过渡”写如何证明这些规则及分阶段迁移安排，包括测试、静态检查和构建等命令，不记录某次运行结果；实施时按第 7 章执行。
* spec 写最终设计，不堆“追加、补丁、前文作废”等讨论过程。重构须区分要改变的行为与保持不变的行为，防止顺手推翻或保留不该保留的旧逻辑。
* current spec 按模块或主题组织，写有效规范与必要示例，不为每篇建立 meta，也不记录任务过程或实际测试日志。

#### 3.2 用户示例与公开契约

* 用户层面的 API、DSL、CLI、配置及结果访问方式属于公开契约，须在 spec 中冻结真实、已确定的示例，不能用伪代码冒充最终写法。关键用户示例放在主 spec 的“公开接口与用户写法”部分，复杂场景可放入明确引用的扩展；无公开面的任务使用真实内部调用或行为示例，不发明 API 凑格式。重要输出、快照或证据也须给最小示例。
* 内部实现可以调整，但不得擅自改变已冻结的用户入口、参数、配置或结果访问方式。改变公开写法须先确认设计并取得修改授权，再同步实现、spec、生成声明和 guide；关键用户写法纳入对应代码任务的离线测试计划，写文档时不运行测试。

#### 3.3 spec 扩展与拆分

* spec 过长时先去重和移除普通实现细节，再判断拆任务还是拆文件：存在可独立实施、验证的目标时拆子任务；同一目标只是接口、算法或示例内容较多时，拆文件即可，不按行数机械增加子任务。
* 正式扩展使用 `spec_01_<topic>.md` 等命名，如 `spec_01_api.md`、`spec_02_algorithm.md`，与主 spec 放在同一任务目录。主文件保持四部分完整，概括整体蓝图、核心规则、最小主链示例和验证范围，不掏空成链接列表；接口、算法与复杂示例的细节放入对应扩展。扩展只展开一个主题，不强制重复四部分，也不复制整套任务边界与验证计划。
* 同一任务的 `spec.md` 和所有 `spec_*.md` 均为必读正式规范，实施前须完整阅读，不存在可选 spec。“按需扩展”只指是否需要创建文件，不表示创建后可以不读。
* 主 spec 在对应部分引用扩展，必要时说明阅读顺序。同目录引用只用文件名，推荐相对 Markdown 链接，不写完整路径；引用负责导航，无需逐处标注“必读规范”。
* 主文件与扩展共同构成该任务的正式规范，同一规则只在一处完整定义，其他位置引用；扩展不是草稿，不另建流程目录或第二套规范。

引用示例：

```md
完整接口与调用示例见 [spec_01_api.md](spec_01_api.md)。
```

### 4. 任务划分、命名与检索

#### 4.1 只有一个主任务

适合目标清楚、能够一起实现和验证的工作：使用一个 task ID、一套 meta/context/spec，在该任务中完成设计、实现和验证，不为简单任务额外拆子任务。

#### 4.2 一个主任务带多个子任务

适合存在明确阶段、依赖或独立验证边界的工作：

* 主任务写整体蓝图、共同约束和阶段职责，提前协调阶段过渡与验证分工；子任务写各自具体目标、范围、设计与验证要求，能够独立指导实施。
* 主任务与每个子任务都有自己的 task ID 和 meta/context/spec，父子关系写在 meta 中，不通过目录嵌套表达。
* 各子任务的验证已覆盖整体承诺时，不再要求主任务重复验证；仍有集成行为或跨任务交互未覆盖时，由合适子任务承接，必要时安排独立验证子任务，尽量在初次拆分时考虑。需要的验证范围直接写进承担它的 task spec，不按任务身份另加统一验证步骤。
* 新建整组任务时，通常先主任务，再按依赖与阶段顺序排列子任务；后续可按实际需要插入或调整版本位置。

#### 4.3 共同规则

**定级**：主任务根据自身设计范围与风险定级；每个子任务根据自己的工作内容独立定级，不继承主任务星级。三星主任务下可以有一星子任务，专门验证的子任务也单独定级。

创建正式任务时将级别写入各自 meta；复杂度实质变化时调整，不因审阅、修复或补测反复定级。星级指导投入，不机械决定目录、子任务数量或测试数量；简单任务的 context 和 spec 可以简短。

| 级别 | 判断依据 |
| --- | --- |
| 零星 | 极小、局部、低风险，不涉及正式契约设计；不建正式任务文档。 |
| 一星 | 简单明确、边界较小的正式任务。 |
| 二星 | 多模块改动，需要明确设计和回归验证。 |
| 三星 | 核心契约、算法、高耦合或破坏性重构，错误影响较大。 |

**编号与目录**：

* task ID 使用 `YYYYMMDD<root>[.<nnn>[A-Z]]`。新主任务按创建日期和大写字母编号；子任务沿用主任务前缀，三位补零，从 `.001` 起。
* `20260911A`、`20260911A.001`、`20260911B`、`20260911B.001A`、`20260911B.001B` 均合法；禁止 `20260911A.001.001` 等多层编号。同一子任务槽位拆成 `.001A/.001B` 后，不再并存该槽位的 `.001`。
* 所有任务目录都直接平铺在 `doc/task_specs/` 下，使用 `<task_id> <英文标题>/`；每个目录承载自己的 meta、context、主 spec 及必要的 spec 扩展。主任务目录不充当子任务容器。

```text
doc/task_specs/
├── 20260911A configuration normalization/
├── 20260911A.001 configuration parsing/
├── 20260911B output normalization/
├── 20260911B.001A output contract/
└── 20260911B.001B output implementation/
```

**顺序与版本边界**：

* 编号日期只作创建标识，子任务沿用主任务前缀；实际依赖与执行顺序以 JJ change/Git commit 图为准，不按名称、日期或提交时间排序。版本位置调整不应为了维持编号排序而重命名任务；`v0.12.3` 等版本号留给发布管理。
* 每个主任务和子任务各有独立版本边界：JJ 默认一任务一个 change，文档和代码可持续在其中完善；Git 可用多个 commit 承载同一 task ID，不强制凑成一个 commit。
* 新建前分别判断归属与插入位置：延续旧主任务还是独立新主任务，应位于哪些前置之后、哪些消费者之前。归属旧主任务不意味着紧挨其历史位置；同范围修复优先维护当前任务，不为文件、审阅或中间过程不断创建任务。
* 跨任务引用只允许下游指向上游。子任务可引用已有主任务和前置；上游不得引用后续任务 ID 或文档，主任务只描述后续阶段职责，不依赖未来子任务解释自身设计。
* 源码和 current spec 链接应在对应 revision 中真实存在，尚未创建的目标路径只能标为计划产物，不能冒充已存在的规范引用。

**检索**：正式文档使用 Markdown；相关任务、前置与替代关系只在当前 meta 中记录，不反向维护旧任务目录索引。

* 当前问题优先读源码、相关 current spec 与 guide。需要任务背景时先查 meta，查取舍读 context，查设计规则读 spec；不默认遍历所有历史文档或读取全部 meta。
* 可以列出任务入口或搜索概括，无需维护第二份手工摘要；项目架构由 current spec 总览表达，不用历史任务目录代替。

```bash
rg --files doc/task_specs -g meta.md
rg -n '配置|解析|校验' doc/task_specs -g meta.md
```

### 5. JJ 优先，Git 回退

* 仓库启用 JJ 时优先使用 JJ，不擅自改用 Git 写历史；未启用时用普通 Git，不主动安装或转换仓库。JJ 便于编辑已有 change、插入前后任务并自动重基后代，适合以任务为单位反复完善的工作流。
* **先确认 change ID，再操作**：使用 JJ 定位、编辑、插入或重基任务前，必须查询当前版本图，核对 change ID、任务描述与上下游关系，再用已确认的 change ID 操作。重基会改变 commit ID，禁止根据记忆或旧日志中的 commit ID 判断任务的当前版本。明确的 commit ID 可用于历史快照取证和差异比较，但不能代替当前任务定位。
* 切换前核对目标并保留当前改动，使用 `jj edit <change-id>` 进入已确认的工作区；定位切换不授权修改任务内容或修复问题。审阅基线按下表选择，普通讨论不默认切换。用户明确禁止切换时保持原位；用户禁用 JJ 时连只读 JJ 命令也不执行。
* **修改必须落在目标 change**：新增或修改受版本控制的内容前，必须确认目标任务的稳定 change ID，并核对当前 `@`。不在目标 change 时，必须先切换并确认，再开始修改；已经在目标 change 时无需重复切换。归属不明确时先确认，禁止为了方便直接改在当前 change 中。本规则覆盖代码、正式任务文档、current spec、guide、AGENTS.md，以及会改写受控文件的格式化或生成操作。
* 仓内 `doc/drafts/` 与用户指定的仓外草稿均不纳入版本控制，不归属任何 change；修改草稿直接按授权处理，不需要切换。
* 正式任务命名为 `task <task_id> <title>`；创建正式任务的指令授权建立相应本地 change/commit。单纯讨论或修改文件不授权额外拆分、合并、搬迁或改变其他任务内容。
* JJ 插入或改写前明确范围、前置、后代与可能冲突；正常重基、内容冲突和任务尾声检查按通用规范第 6 章处理，使用已确认的 change ID 追踪后代。
* Git 项目可以继续旧主任务的子任务编号，默认在当前开发分支顶部迭代。在已授权的任务整理范围内，快速迭代可对最近少量、影响清楚的本地 commit 做简单重基；不做久远或跨大量后代的复杂历史重写，不为凑一个 commit 而重基。涉及已发布或共享提交时须先确认影响并明确获准，不能推定有强推权限。
* 推送、强推、远端/发布线调整、破坏性恢复或删除不包含在普通任务授权中，必须明确获准。操作后说明 change/commit、修改范围、重基与残余冲突；不维护冗长的通用历史重写操作手册。

**审阅范围与版本基线分开**：“整个项目”描述范围，“当前／最新”决定版本，不能混为一谈。

| 审阅对象 | JJ 工作区与基线 |
| --- | --- |
| 指定任务或 change | 确认稳定 change ID 后，默认切到该任务的当前版本，判断其自身交付。 |
| 主任务及全部子任务 | 按实际依赖选择覆盖整组成果的集成版本，通常是最后一个相关子任务；必要时查看各阶段差异，不能只进入蓝图主任务。 |
| 当前源码、当前工作区 | 保持当前 change，以当前内容为基线。 |
| 最新源码、最新项目状态 | 查询当前版本图及开发书签／分支配置，定位最新开发栈；不在目标位置时先切换。不能凭记忆、任务编号或时间戳猜“最新”。 |
| 草稿，无论仓内 `doc/drafts/` 或用户指定的仓外目录 | 两者都不纳入版本控制，直接审阅实际文件，不因草稿审阅切换 change。 |

判断任务自身与判断该能力在当前／最新集成版本是否正常，是不同审阅目标。
源码、文档和测试须明确对应基线；追溯其他版本时单独说明，不能把后代修复当成原任务本来就正确。

### 6. 冲突判断与任务边界

**已有设计能确定行为、且属于当前授权范围的问题，自动处理；需要新设计决定或扩大任务范围的问题，先暂停确认。** 判断看职责和语义，不看改动大小、文件年龄或能否自动合并。

* **范围内自主处理**：普通实现选择、明确规范下的缺陷和机械适配，不必逐项询问；不得破坏明确要求保留的行为或覆盖用户独立改动。
* **核心阻断必须确认**：设计蓝图存在重要遗漏、真值不明，或者修复会改变未授权的算法、契约、使用场景及迁移边界时，不能自行补设计。
* **上游修复单独授权**：修改当前任务负责的历史文件不算越界；临时发现必须修复另一个上游任务才能继续，则须确认，不能把修复藏进当前 change。已明确授权的整组任务按计划执行，不重复询问。
* **正常重基自动处理**：任务尾声检查后代、冲突和受影响验证，过程中不反复整理历史。重基只是继承已批准的修改，不授权修复新发现的范围外缺陷，也不扩大只读任务权限。
* **暂停后清楚交接**：保留有效修改，必要时只读定位，简要说明问题、越界原因、推荐处理和所需授权。不得靠临时兼容、弱化校验或测试掩盖阻断。“赞同”“可以”及单独“继续”不扩大授权，收到明确的新范围执行指令后再恢复。

### 7. 实现、验证与审阅

* 先读懂并复用已有入口、类型、注册、配置、错误和测试体系。每个核心契约只有一个真值源；新能力扩展正式链路，不平行再造，不用临时 helper 冒充正式功能。
* 被替代的旧接口、兼容回退、镜像类型与旧用法按 task spec 约定的阶段退出；已规划的临时桥接在限定范围内保留至约定阶段，不当作当前必须立即删除的残留，也不能变成长期平行实现。旧用法正式退出时，按对应任务的验证安排证明其不能再静默通过，覆盖代码、测试、生成声明、示例和文档。
* 代码直接表达业务；仅为消除真实复杂度、重复或稳定边界增加抽象。不隐藏控制流，不加没有真实场景的防御。对非法输入和实际边界明确报错，不静默回退；依赖的上游约束在必要处说明。
* 源码检索使用 `rg → ast-grep → 精确阅读`：先缩小范围，大型重构主动用语法结构枚举候选，再读完整函数、类型及调用者。ast-grep 不证明类型、符号绑定、宏展开或跨模块语义；同时用 rg 防漏。
* ast-grep 默认一次性、只读、限定路径；run 使用 `--threads 1`，不默认使用 rewrite、update-all、交互编辑、scan、lsp 或 MCP。不为导航擅自启动常驻索引服务。
* 正式任务的代码实施按 task spec 预先约定的范围、命令和通过要求验证；测试范围是任务设计的一部分，不因主任务、子任务或星级标签临时扩大或缩小。使用仓库正式入口并遵守项目资源约束，不以手拆底层命令冒充验证；纯文档任务遵守只核对文档的默认规则。
* 必须通过的验证失败不能自动归入后续处理；已明确接受且符合约定的阶段性失败，不反复报告成新阻断。新出现的失败先定位原因，涉及设计遗漏或范围外问题时按第 6 章确认，不能先改 spec 添加免责范围。
* 默认只新增离线自动化测试，使用本地数据、fixture 或 mock。只有用户明确要求时才新增在线测试；在线与离线测试必须使用独立入口，默认离线测试不得触发真实网络或外部服务。
* 在线测试、调试等请求应设置合理的超时与有限重试，等待时间一般不要过长。
* 遇到明显异常的超时或反复失败，AI 应主动停止相关在线工作并告知用户，不机械地继续等待、重试。
* 编译耗时长本身不是异常，应持续等待，不因太慢或暂时没有输出而终止。
* 测试证明契约、不变量、等价类、边界和错误，而非实现细节或数量。正向测试证明合法输入按预期成功；反向测试证明非法输入或不满足前置条件时按预期报错，断言正确的错误类型、阶段和必要上下文，不能“随便报错就算通过”。反向用例服从已有契约，不为凑测试发明新的报错行为。
* 预期报错的反向用例符合断言时仍是测试通过；它与迁移期间接受的测试失败或暂不可执行不是一回事，不得混淆。
* fixture、mock、调用与预期必须对应真实行为。禁止空断言、只测 mock 自己、猜测不存在的 API、编造结果或削弱断言求通过。优先小样本、固定 seed 和可重复测试，组合本身有风险才使用矩阵。
* 验证时检查失败、warning、deprecation、异常跳过和 summary，不只看退出码。先定位根因，按第 6 章确认修复范围；无法处理的第三方警告或验证限制明确说明影响，不宣称干净通过。
* 修改状态的操作和正式验证有序执行；不边改被验证的代码边测试，不因暂时无输出就重启或杀进程。在线诊断、临时探针放专用 debug 目录，显式运行，不混入默认测试或正式主链；不再使用的探针及时收拾，但不把草稿当探针删除。
* 审阅先按第 5 章确定范围和实际基线。文档审阅只判断是否能指导实施，报告核心阻断；代码审阅沿真实调用、数据、状态、错误和生命周期检查正确性、安全、性能、维护性与旧残留，不只看 diff 或测试是否通过。
* 同根因合并处理；职责、阶段或范围混淆先澄清，不拆成无尽局部返工。满足本次范围与验证要求后停止，不以偏好、额外 guard 或未来增强拖延交付。

### 8. 当次回复规范

* AI 的阶段总结、任务总结和审阅报告只在当次回复提供，不默认落盘；不创建 review、实施总结或验收记录等替代文件，也不把临时执行记录、测试统计或进度塞进 meta/context/spec。规范中仍须保留验证要求。
* 回复按场景简洁组织，不强制固定七节或无内容栏目：

| 场景 | 回复内容 |
| --- | --- |
| 纯文档任务 | 通常一句完成确认即可；必要时简述主要修改及未处理事项，不重复整篇文档。 |
| 代码实现或修复 | 说明实现范围、实际验证结果、未验证项与剩余问题，以及相关 change/commit 和重基情况。 |
| 审阅 | 说明实际审阅基线，给出总体判断、有证据的问题与建议、人工选择项和重要未覆盖范围；无问题时明确说明。 |

* “本次范围已实现；相关测试通过；未运行全量测试。”只能作为当次事实回复，不能写入任务文档。不得把未检查、未运行或有未解释警告的范围说成已验证。
* 审阅问题给出位置、原因、影响和可落地建议，区分 AI 可直接修与人工拍板。必要时用 P1/P2/P3 表达阻断、高风险和低风险，不另设决策编号；风险级别与任务复杂度星级分开。关键分叉给推荐与取舍。

### 9. 文件行数与拆分

| 文件 | 硬上限 |
| --- | ---: |
| `meta.md` | 60 行 |
| context、spec（含扩展）、current spec、guide 与其他手写文档 | 400 行 |
| 手写代码，包括测试、脚本、debug | 400 行 |
| `AGENTS.md` | 不设硬上限；规则集中去重，不为压行数删除必要约束。 |

* 按物理行数计，空行和注释也计入。生成文件、第三方代码和纯数据文件明确豁免，但来源须可识别，不能借此隐藏手写代码。
* 有上限的新文件及原本未超限的本轮修改文件必须遵守上限。历史超限文件不因规则更新自动全仓拆分；涉及当前修改时说明行数和影响，拆分由本次范围决定，不夹带无关重构。
* 超限优先按职责机械拆分、简单封装或转发，保持接口、控制流、错误和结果等价，除非任务明确要求改变。禁止为了压行数引入框架、配置层、空壳模块或超长单行。
* 文档拆分不丢关键语义、不复制多份规范；代码注释解释约束与易误读点，不复述代码，修改时同步清理过时注释。

## 二、本项目规范
